apiVersion: v1
kind: Service
metadata:
  name: order-api
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/path: "/metrics"
    prometheus.io/port: "80"
spec:
  selector:
    app: order-api
  ports:
    - port: 80
      targetPort: 5000
  type: ClusterIP
